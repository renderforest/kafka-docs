address: USER-MOCKUP-CREATE
description: Channel for user mockup project creation events
messages:
 MockupProjectCreatedEvent:
    contentType: application/json
    payload:
      $ref: '#/components/schemas/MockupProjectSchema'